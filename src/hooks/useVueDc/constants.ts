/**
 * root 根组件的glob prop injection key
 * @type {Symbol}
 */
export const DC_ROOT_KEY: symbol = Symbol('DC_ROOT_KEY')

/**
 * 组件的modelValues prop injection key of glob root
 * @type {Symbol}
 */
export const DC_MODELVALUES_KEY: symbol = Symbol('DC_MODELVALUES_KEY')

/**
 * 判断是否是数据回显的 injection key
 * @type {Symbol}
 */
export const DC_RENDER_BACK_KEY: symbol = Symbol('DC_RENDER_BACK_KEY')
